import { OpneProductDetailsDirective } from './opne-product-details.directive';

describe('OpneProductDetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new OpneProductDetailsDirective();
    expect(directive).toBeTruthy();
  });
});
